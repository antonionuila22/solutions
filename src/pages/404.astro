---
import Layout from "../layouts/Layout.astro";
import Button from "@/components/Button.astro";
---

<Layout
  title="404 - Page Not Found | Codebrand"
  description="The page you're looking for doesn't exist or has been moved. Return to Codebrand's homepage to explore our web development, design, and digital marketing services."
  keywords="404 error, page not found, codebrand, digital agency"
  image="/photos/bannercodebrand.webp"
  noindex={true}
>
  <section
    class="relative min-h-screen flex flex-col items-center justify-center text-center px-6 py-24 bg-cyan-900 overflow-hidden"
  >
    <!-- Animated background elements -->
    <div class="absolute inset-0 overflow-hidden">
      <div
        class="absolute w-[600px] h-[600px] bg-cyan-400/20 rounded-full blur-[120px] opacity-40 top-[-200px] left-[-200px] animate-pulse"
      ></div>
      <div
        class="absolute w-[500px] h-[500px] bg-orange-500/20 rounded-full blur-[100px] opacity-30 bottom-[-150px] right-[-150px] animate-pulse"
        style="animation-delay: 1s;"
      ></div>
      <div
        class="absolute w-[300px] h-[300px] bg-orange-500/15 rounded-full blur-[80px] opacity-40 top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2"
      ></div>
    </div>

    <!-- Grid pattern overlay -->
    <div class="absolute inset-0 bg-[url('/grid.svg')] bg-center opacity-5"></div>

    <div class="relative z-10 max-w-4xl mx-auto">
      <!-- 404 Number with glitch effect -->
      <div id="number" class="relative mb-8">
        <h1
          class="text-[140px] md:text-[200px] lg:text-[240px] font-black text-orange-500 leading-none select-none"
        >
          404
        </h1>
        <!-- Glitch layers -->
        <h1
          class="absolute inset-0 text-[140px] md:text-[200px] lg:text-[240px] font-black text-cyan-400/30 leading-none select-none glitch-1"
          aria-hidden="true"
        >
          404
        </h1>
        <h1
          class="absolute inset-0 text-[140px] md:text-[200px] lg:text-[240px] font-black text-orange-500/30 leading-none select-none glitch-2"
          aria-hidden="true"
        >
          404
        </h1>
      </div>

      <!-- Main message -->
      <div id="message" class="space-y-4 mb-10">
        <h2 class="text-2xl md:text-3xl lg:text-4xl font-bold text-white">
          Oops! Page not found
        </h2>
        <p class="text-lg md:text-xl text-gray-400 max-w-2xl mx-auto">
          The page you're looking for seems to have wandered off into the digital void.
          Don't worry, let's get you back on track.
        </p>
      </div>

      <!-- Action buttons -->
      <div id="buttons" class="flex flex-col sm:flex-row gap-4 justify-center mb-16">
        <a
          href="/"
          class="inline-flex items-center justify-center px-8 py-4 bg-orange-500 hover:bg-orange-600 text-white font-semibold rounded-xl hover:shadow-lg hover:shadow-orange-500/25 transition-all duration-300 hover:scale-105"
        >
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
          </svg>
          Back to Home
        </a>
        <a
          href="/contact"
          class="inline-flex items-center justify-center px-8 py-4 border-2 border-white/30 text-white font-semibold rounded-xl hover:border-orange-500 hover:text-orange-500 transition-all duration-300 hover:scale-105"
        >
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
          </svg>
          Contact Us
        </a>
      </div>

      <!-- Quick links -->
      <div id="links" class="border-t border-white/20 pt-10">
        <p class="text-white/60 mb-6">Or check out these popular pages:</p>
        <div class="flex flex-wrap justify-center gap-3">
          <a
            href="/web-development"
            class="px-4 py-2 bg-white/10 text-white/80 rounded-lg hover:bg-orange-500 hover:text-white transition-all duration-200 text-sm"
          >
            Web Development
          </a>
          <a
            href="/branding"
            class="px-4 py-2 bg-white/10 text-white/80 rounded-lg hover:bg-orange-500 hover:text-white transition-all duration-200 text-sm"
          >
            Branding
          </a>
          <a
            href="/e-commerce"
            class="px-4 py-2 bg-white/10 text-white/80 rounded-lg hover:bg-orange-500 hover:text-white transition-all duration-200 text-sm"
          >
            E-Commerce
          </a>
          <a
            href="/seo"
            class="px-4 py-2 bg-white/10 text-white/80 rounded-lg hover:bg-orange-500 hover:text-white transition-all duration-200 text-sm"
          >
            SEO
          </a>
          <a
            href="/blog"
            class="px-4 py-2 bg-white/10 text-white/80 rounded-lg hover:bg-orange-500 hover:text-white transition-all duration-200 text-sm"
          >
            Blog
          </a>
          <a
            href="/projects"
            class="px-4 py-2 bg-white/10 text-white/80 rounded-lg hover:bg-orange-500 hover:text-white transition-all duration-200 text-sm"
          >
            Projects
          </a>
        </div>
      </div>
    </div>
  </section>

  <style>
    @keyframes glitch-1 {
      0%, 100% { transform: translate(0); }
      20% { transform: translate(-2px, 2px); }
      40% { transform: translate(-2px, -2px); }
      60% { transform: translate(2px, 2px); }
      80% { transform: translate(2px, -2px); }
    }

    @keyframes glitch-2 {
      0%, 100% { transform: translate(0); }
      20% { transform: translate(2px, -2px); }
      40% { transform: translate(2px, 2px); }
      60% { transform: translate(-2px, -2px); }
      80% { transform: translate(-2px, 2px); }
    }

    .glitch-1 {
      animation: glitch-1 3s infinite;
    }

    .glitch-2 {
      animation: glitch-2 3s infinite;
      animation-delay: 0.1s;
    }
  </style>

  <script type="module" is:inline>
    import gsap from "https://cdn.skypack.dev/gsap";

    gsap.from("#number", {
      opacity: 0,
      scale: 0.5,
      y: -50,
      duration: 1.2,
      ease: "elastic.out(1, 0.5)",
    });

    gsap.from("#message", {
      opacity: 0,
      y: 30,
      delay: 0.4,
      duration: 0.8,
      ease: "power3.out",
    });

    gsap.from("#buttons", {
      opacity: 0,
      y: 20,
      delay: 0.6,
      duration: 0.8,
      ease: "power3.out",
    });

    gsap.from("#links", {
      opacity: 0,
      y: 20,
      delay: 0.8,
      duration: 0.8,
      ease: "power3.out",
    });
  </script>
</Layout>
