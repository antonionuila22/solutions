---
import ReactBanner from "../components/reactBanner.tsx";
const { title, subtitle, description, imgUrl } = Astro.props;
---

<ReactBanner
    title={title}
    subtitle={subtitle}
    description={description}
    imgUrl={imgUrl}
    client:only="react"
/>
