---
import SplitText from "../components/SplitText.tsx";
import Button from "../components/Button.astro";
import Background from "../components/background.tsx";
const handleAnimationComplete = () => {
    console.log("¡Todas las letras han terminado de animarse!");
};
---

<section class="flex items-center justify-center flex-col my-25">
    <div class="container px-4 py-10 flex items-center justify-center w-full">
        <article
            class="relative flex w-full text-center bg-cover bg-center rounded-[20px] py-[5vw] gap-4 items-center justify-center flex-col"
        >
            <div class="absolute mt-[-10em] w-full h-full opacity-60">
                <Background client:visible />
            </div>

            <h1
                class="relative max-w-6xl text-md lg:text-xl sm:text-sm text-orange-400 animate-fade-in"
            >
                Design experiences - Develop results
            </h1>

            <SplitText
                text="Codebrand"
                className="text-cyan-900 font-bold max-w-5xl lg:max-w-6xl text-5xl lg:text-8xl md:text-6xl sm:text-4xl max-h-[650px]"
                client:visible
            />

            <p
                class="relative text-lg text-black max-w-5xl animate-fade-in opacity-0 transition-opacity duration-700 delay-200"
            >
                Time. Control. Results. We simplify your operations, increase
                your visibility, and automate your processes so you can focus on
                what matters.
            </p>

            <div
                class="relative flex flex-col md:flex-row gap-7 flex-wrap justify-center z-1"
            >
                <Button text="Grow my business Now!" href="./contact" />
            </div>
        </article>
    </div>
</section>
