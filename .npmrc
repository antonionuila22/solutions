; Intentionally empty.
; This repo is pnpm only (see package.json "packageManager" and pnpm-lock.yaml).
; It does NOT set legacy-peer-deps: nothing here needs it, and it is an npm only
; flag that pnpm ignores. Install with "pnpm install", audit with "pnpm audit".
